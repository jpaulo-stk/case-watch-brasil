# Banco de dados (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=
DB_NAME=case_watch

# Segredo para assinar o JWT (use uma string longa e aleatória)
JWT_SECRET=

# Opcional — porta da API (padrão 3000)
# PORT=3000
