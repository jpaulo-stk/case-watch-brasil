# URL da API.
# Local: pode deixar como está (ou comentar — o padrão já é http://localhost:3000).
# Produção: aponte para o endpoint do deploy (ex.: API Gateway / Lambda).
VITE_API_URL=https://wabn5zl24h.execute-api.us-east-1.amazonaws.com

